/**
 * firebase.js — STUB (Firebase eliminado).
 * Este proyecto usa exclusivamente Supabase + Cloud Run + Vercel.
 * Archivo mantenido como no-op para no romper imports residuales.
 */
export const auth = null;
export const db = null;
export const storage = null;
