/**
 * Placeholder: pruebas de reglas con emulador.
 * Ejecutar: firebase emulators:exec "node --test tests/firestore-rules.test.js"
 * (requiere @firebase/rules-unit-testing en el proyecto si se amplía)
 */
'use strict';

const { describe, it } = require('node:test');
const assert = require('node:assert');

describe('firestore.rules', () => {
  it('placeholder — desplegar reglas y validar en consola / emulador', () => {
    assert.ok(true);
  });
});
